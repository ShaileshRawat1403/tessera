[pytest]
# Real tests live in each package's tests/ dir. examples/ holds sample inputs
# for the packs (some of which contain test_*.py fixtures that are data, not
# our suite), so it must not be collected.
testpaths = packages tests
norecursedirs = examples .venv dist build *.egg-info
