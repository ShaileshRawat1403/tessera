# API docs style guide

Each endpoint doc follows the structure:

1. Short summary (one sentence)
2. Method + path in a fenced block
3. Path parameters table (name, type, required, description)
4. Request body schema reference
5. Response body schema reference
6. One worked example
